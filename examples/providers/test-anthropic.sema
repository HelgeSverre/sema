;; Test Anthropic provider
(llm/configure :anthropic {:api-key (env "ANTHROPIC_API_KEY")})

(define result
  (llm/complete "Say 'hello from Anthropic' and nothing else." {:max-tokens 20}))
(println "Anthropic complete:" result)

(print "Anthropic stream: ")
(llm/stream "Count from 1 to 5, one number per word, nothing else."
  {:max-tokens 30})
(newline)

(println "Anthropic: OK")
